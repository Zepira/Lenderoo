#use this later: https://expo.dev/blog/how-to-integrate-eas-workflows-with-github-actions

name: Deploy Web to Expo

on:
  push:
    branches:
      - main
      - master
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: "pages"
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Setup Yarn
        run: corepack enable

      - name: Install dependencies
        run: yarn install --immutable

      - name: Setup Expo and EAS
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}
      - name: Install dependencies
        run: yarn install --frozen-lockfile

      - name: Export web build
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
          EXPO_PUBLIC_HARDCOVER_API_TOKEN: ${{ secrets.EXPO_PUBLIC_HARDCOVER_API_TOKEN }}

        run: npx expo export --platform web

      - name: Deploy Expo
        env:
          EXPO_PUBLIC_SUPABASE_URL: ${{ secrets.EXPO_PUBLIC_SUPABASE_URL }}
          EXPO_PUBLIC_SUPABASE_ANON_KEY: ${{ secrets.EXPO_PUBLIC_SUPABASE_ANON_KEY }}
          EXPO_PUBLIC_HARDCOVER_API_TOKEN: ${{ secrets.EXPO_PUBLIC_HARDCOVER_API_TOKEN }}

        run: eas deploy --alias lenderoo-web
# Source - https://stackoverflow.com/a
# Posted by Lavínia Beghini
# Retrieved 2026-01-21, License - CC BY-SA 4.0

