# Hardcover API Token (optional but recommended)
# Get your token at: https://hardcover.app/account
EXPO_PUBLIC_HARDCOVER_API_TOKEN=

# Dev auto-login (development only — never set in production)
# Create a matching user in your Supabase project's Authentication dashboard.
EXPO_PUBLIC_DEV_EMAIL=
EXPO_PUBLIC_DEV_PASSWORD=
